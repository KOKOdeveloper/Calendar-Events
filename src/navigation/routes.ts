export enum RoutesNavigators {
  auth = 'AuthNavigator',
  main = 'MainNavigator',
}

export enum RoutesAuth {
  pending = 'PendingScreen',
  signIn = 'SignInScreen',
  signUp = 'SignUpScreen',
}

export enum RoutesMain {
  calendar = 'CalendarScreen',
  profile = 'ProfieScreen',
  event = 'EventScreen',
}
